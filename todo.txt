Ive lost my local todo list, so this is very outdated, but ill try to add things as i realize them


Projects to make:
 - connective-standalone: rewrite of the standalone server, new module loading etc
 - orusn: load balancer server (protocol is called crystal)
 - connective-security: security library implementing JWT and various tools as well as contextfile restrictions in jwt and basic auth
 - jazzcode: rewrite of the jazzcode module with better support and security
 - connective-websockets: websocket server implementation
 - connective-cgi: cgi support


TODO:
 - connective-http:
   - make sure protocol switching is handled better
     - make sure the server properly handles protocol switched clients, that it remembers the client handler
     - make sure the protocol switcher can mark the client as closed
     - make sure protocol switcher can handle a server stop event to disconnect the client
     - make sure server stop gives clients a 30 second timer to cleanly stop and forces stop if taking too long
     - make sure force quit fully closes upgraded clients
   - proper support for OPTIONS methods and CORS, with OptionsHandler interface, providing headers for options requests without running handlers, a server-wide, contentsource-specific and handler-specific OptionsHandler, with of course support for the lambda types
   - builtin support for VHosts with fallback
   - support for byte range (partial file) requests
   - HTTP/2 support
   - HTTP/3 support
